import numpy as np
import data

def cut_parts(parts_num, data):
    for i in parts_num:
        labeled = np.array[z][x][y]
        min[parts_num] = (z[m],x[m],y[m])
        max[parts_num] = (z[m],x[m],y[m])
        parts[parts_num] = data(min[parts_num]:max[parts_num])
    return parts

def label_segmentaion(voxel):
